import { Size } from "../math/Geometry";

export const DEFAULT_CELL_SIDE_LENGTH = 48;
export const DEFAULT_CELL_SIZE = new Size(DEFAULT_CELL_SIDE_LENGTH);

export const DEFAULT_BOARD_SIDE_LENGTH = 3;
export const DEFAULT_BOARD_SIZE = new Size(DEFAULT_BOARD_SIDE_LENGTH);
export const DEFAULT_WINNING_LINE_LENGTH = DEFAULT_BOARD_SIDE_LENGTH;